name: "Hello World"

on:
  push:
    branches:
      - 'master'
  pull_requests:
    branches:
      - 'master'

jobs:
  hello:
    runs-on: ubuntu-latest

  steps:
    - name: Hello Step
      run: echo "Hello World"